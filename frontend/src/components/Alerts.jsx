import { AlertTriangle, CheckCircle2 } from "lucide-react";
export default function Alerts({data,risk}){if(!data)return null;const good=data.class_name==='SAFE';return <div className="rounded-[2rem] bg-white p-5 shadow-lg border"><div className="flex items-center gap-2 font-black text-slate-900">{good?<CheckCircle2 className="text-green-600"/>:<AlertTriangle className="text-yellow-600"/>} Alerts & explanation</div><p className="mt-2 text-sm text-slate-600">{data.recommendation}</p><div className="mt-4 space-y-2">{(risk?.why||data.explanations).map((e,i)=><div key={i} className="rounded-xl bg-slate-50 p-3 text-sm"><b>{e.reason}</b><p className="text-slate-500">{e.impact} · {e.severity}</p></div>)}</div></div>}

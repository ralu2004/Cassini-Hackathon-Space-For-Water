import { Droplets, Navigation, WifiOff } from "lucide-react";
export default function BottleStatus({data,onScan,loading}){const color=data?.color||"#0ea5e9";return <section className="rounded-[2rem] p-5 shadow-xl glass border border-white/60"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[.3em] text-slate-500">Terra Sip</p><h1 className="text-3xl font-black text-slate-900">Smart Bottle</h1></div><div className="h-16 w-10 rounded-b-3xl rounded-t-lg border-4 flex items-end overflow-hidden" style={{borderColor:color}}><div className="w-full transition-all" style={{height:`${Math.max(18,data?.wqi||50)}%`,background:color}} /></div></div><div className="mt-6 flex items-center gap-4"><div className="h-24 w-24 rounded-full flex items-center justify-center text-white shadow-lg" style={{background:color}}><Droplets size={42}/></div><div><p className="text-sm text-slate-500">Water Quality</p><h2 className="text-4xl font-black" style={{color}}>{data?.drinkability||"SCAN"}</h2><p className="text-slate-700 font-medium">{data?`${data.wqi}/100 — ${data.class_name}`:"Tap to scan nearby water"}</p></div></div><button onClick={onScan} disabled={loading} className="mt-6 w-full rounded-2xl bg-slate-950 py-4 text-white font-bold flex items-center justify-center gap-2 active:scale-[.99]"><Navigation size={18}/>{loading?"Scanning with high-accuracy GNSS…":"Tap to scan location"}</button><div className="mt-3 flex items-center gap-2 text-xs text-slate-500"><WifiOff size={14}/> Offline cache enabled: last scan is stored locally.</div></section>}
